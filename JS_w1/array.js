const initialArray = [1, 2, 3, 4, 5];

function logThing(str) {
    console.log(str);
}
// for each
initialArray.forEach(logThing); // foreach takes a function it takes function as argument and implement in each element of that array



